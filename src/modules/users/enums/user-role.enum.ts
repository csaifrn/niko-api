// TODO: consultar as funções de cada um e ver o que o cliente há;
// TODO: checar se o cliente possui mais privilégios que o MANAGER
export enum UserRole {
  OPERATOR = 1,
  COORDINATOR = 2,
  MANAGER = 3,
  CLIENT = 4,
}
